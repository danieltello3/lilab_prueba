<div class="card">

   <div class="card-body">
      <h5 class="card-title">Cliente:
         {{prestamoProcesado.cliente.clienteNombre}}
         {{prestamoProcesado.cliente.clienteApellido}}</h5>
      <p class="card-text">Monto Solicitado:
         {{prestamoProcesado.prestamoMonto}}$</p>
      <p class="card-text"><strong>Estado:
            {{prestamoProcesado.prestamoEstado}}</strong></p>
      <div class="card-header">
         Criterios de aceptación
      </div>
      <ul class="list-group list-group-flush">
         <li class="list-group-item">Deuda registrada SBS:
            {{prestamoProcesado.montoSBS}}</li>
         <li class="list-group-item">Indicador Sentinel:
            {{prestamoProcesado.indSentinel}}</li>
         <li class="list-group-item">puntaje de Algoritmo de IA:
            {{prestamoProcesado.algoritmoIA}}</li>
      </ul>
      {{! La idea inicial era actualizar el estado del prestamo con un patch, como lo indica la ruta, pero no es posible directamente por handlebars.
      en React si podria realizarse}}
      <a href="#" class="btn btn-success">Aprobar</a>
      <a href="#" class="btn btn-danger">Denegar</a>
   </div>
</div>